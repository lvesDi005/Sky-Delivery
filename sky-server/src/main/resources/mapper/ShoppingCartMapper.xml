<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
    <insert id="insertBatch">
        insert into shopping_cart (name, image, dish_id, setmeal_id, dish_flavor, number, amount, create_time, user_id)
        values
        <foreach collection="shoppingCartList" item="item" separator="," open="(" close=")" index="index">
            #{item.name},#{item.image},#{item.dishId},#{item.setmealId},#{item.dishFlavor},#{item.number},#{item.amount},#{item.createTime},#{item.userId}
        </foreach>
    </insert>
    <delete id="deleteByUserId">
        delete from shopping_cart
        <where>
            user_id = #{userId}
        </where>
    </delete>
    <delete id="deleteById">
        delete from shopping_cart
        <where>
            id = #{id}
        </where>
    </delete>

    <select id="list" parameterType="com.sky.entity.ShoppingCart" resultType="com.sky.entity.ShoppingCart">
        select * from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
    </select>
</mapper>
